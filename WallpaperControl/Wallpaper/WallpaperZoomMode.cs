namespace WallpaperControl
{
    /// <summary>
    /// Selects the zoom direction used by the wallpaper zoom-and-fade effect.
    /// </summary>
    internal enum WallpaperZoomMode
    {
        In,
        Out
    }
}
