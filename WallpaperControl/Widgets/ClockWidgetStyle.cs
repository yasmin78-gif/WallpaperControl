namespace WallpaperControl
{
    /// <summary>
    /// Identifies clock appearances; numeric values are persisted in the registry and must remain stable.
    /// </summary>
    internal enum ClockWidgetStyle
    {
        Minimal = 0,
        Chrome = 1,
        Clean = 2,
        Glow = 3,
        Classic = 4
    }
}
